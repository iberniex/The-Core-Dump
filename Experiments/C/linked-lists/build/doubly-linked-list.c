#include <stdlib.h>

struct Node {
  int data;
  struct Node *previous;
  struct Node *next;
} Node;

int main(int argc, char *argv[]) {

  char *ready = malloc(40);

  struct Node *start;
  {
    int a = 1;
    0;
    0;
  };

  struct Node end;
  int *start.next = &end;

  return EXIT_SUCCESS;
}
